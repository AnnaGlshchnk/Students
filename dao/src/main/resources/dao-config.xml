<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder ignore-unresolvable="true" location="classpath:sql.properties"/>


    <bean name="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${user.driverClassName}" />
        <property name="url" value="${user.url}" />
        <property name="username" value="${username.name}" />
        <property name="password" value="${user.password}" />
    </bean>

    <beans profile="default">
        <context:property-placeholder location="classpath:app.properties"/>
    </beans>

    <beans profile="docker">
        <context:property-placeholder location="classpath:app-docker.properties"/>
    </beans>


</beans>